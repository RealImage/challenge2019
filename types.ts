export interface inputObject {
    deliveryID:string,
    deliverySize:Number,
    theatreID:string
}

export interface partnersObject {
    theatreID:string,
    slabMinSize:Number,
    slabMaxSize:Number,
    minimumCost:Number,
    costPerGB:Number,
    partnerID:string
}
